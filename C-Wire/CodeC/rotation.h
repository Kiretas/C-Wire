#ifndef ROTATION_H
#define ROTATION_H

#include "consumer.h"

Consumer* rotationGauche(Consumer* a);
Consumer* rotationDroite(Consumer* a);
Consumer* doubleRotationGauche(Consumer* a);
Consumer* doubleRotationDroite(Consumer* a);

#endif
